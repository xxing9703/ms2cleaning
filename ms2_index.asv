% automatically generate an index file
% collecting all .mzXML files and inclusion list .csv under the selected folder
% note:  there shouldn't be

function ms2_index(fn_index)
if nargin<1
    fn_index='index.xlsx';
end
folder=uigetdir();
A=dir(fullfile(folder,'*.mzXML'));
B=dir(fullfile(folder,'*.csv'));
A1=fullfile({A.folder},{A.name});
B1=fullfile({B.folder},{B.name});
out=[A1',B1'];
writecell(out,fullfile(folder,fn_index))